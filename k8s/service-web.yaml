# ASSUMPTION: Fastify listens on port 3000 (matches src/domain/config.ts
# `PORT` default and deployment-web.yaml's containerPort). src/web/server.ts
# does not exist yet — reconcile if the real server binds elsewhere.
apiVersion: v1
kind: Service
metadata:
  name: ai-review-web
  namespace: enac-ai-reviewer
  labels:
    app.kubernetes.io/name: ai-review-web
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: enac-ai-reviewer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-review-web
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP

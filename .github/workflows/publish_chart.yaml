name: Package and Publish Helm Chart

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      registry:
        required: true
        type: string
    outputs:
      chart_version:
        description: "The version of the published chart"
        value: ${{ jobs.build-image.outputs.chart_version }}

jobs:
  build-image:
    name: Package and Push Chart
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    defaults:
      run:
        working-directory: ./helm
    outputs:
      chart_version: ${{ steps.chart_version.outputs.chart_version }}
    steps:
      - uses: actions/checkout@v4

      - name: Log into registry ${{ inputs.registry }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io/${{ inputs.registry }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Render Chart
        # Smoke-test both secret paths. Default (existingSecret) needs Secret
        # names and disables embedded Postgres; chart-managed (dev) needs
        # throwaway values including a Postgres password. Real deploys inject
        # the real values.
        run: |
          helm template . \
            --set web.existingSecret.name=ci-web-secret \
            --set worker.existingSecret.name=ci-worker-secret \
            --set database.postgresql.enabled=false
          helm template . \
            --set web.existingSecret.enabled=false --set web.secrets.GITHUB_WEBHOOK_SECRET=ci-webhook-secret \
            --set worker.existingSecret.enabled=false --set worker.secrets.GITHUB_APP_ID=1 --set worker.secrets.GITHUB_PRIVATE_KEY=ci-pem --set worker.secrets.LLM_API_KEY=ci-llm-key \
            --set database.postgresql.auth.password=ci-postgres-password

      - name: Set Helm chart version based on branch
        run: |
          if [[ "${{github.ref}}" == refs/tags/* ]]; then
            echo "CHART_VER=1.0.${{ github.run_number }}" >> $GITHUB_ENV
          fi
          if [[ "${{github.ref}}" == refs/heads/stage ]]; then
            echo "CHART_VER=1.0.${{ github.run_number }}-rc" >> $GITHUB_ENV
          fi
          if [[ "${{github.ref}}" == refs/heads/dev ]]; then
            echo "CHART_VER=1.0.${{ github.run_number }}-dev" >> $GITHUB_ENV
          fi
          if [[ "${{github.ref}}" == refs/heads/ci-test/* ]]; then
            echo "CHART_VER=1.0.${{ github.run_number }}-dev" >> $GITHUB_ENV
          fi

      - name: Package Chart
        run: helm package --version $CHART_VER .

      - name: Push Chart
        if: github.event_name != 'pull_request'
        run: helm push ${{ inputs.name }}-$CHART_VER.tgz oci://ghcr.io/${{ inputs.registry }}/helm
        env:
          HELM_EXPERIMENTAL_OCI: 1

      - name: Return Chart Version
        id: chart_version
        run: |
          echo "chart_version=$CHART_VER" >> $GITHUB_OUTPUT
